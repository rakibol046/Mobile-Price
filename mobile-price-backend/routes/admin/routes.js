const express =  require('express');
const Route = express.Router();

// Route.get("/phones", allPhones);
// Route.get("/phones/:deviceId", phone);
// Route.get("/laptops", getLaptop);
// Route.get("/watch", getWatch);









module.exports = {
    Route,
}